name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: learnquest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20.x"
          cache: "npm"
          cache-dependency-path: learnquest/package-lock.json

      - name: Install root dependencies
        run: npm install --ignore-scripts --no-audit --no-fund

      - name: Install backend dependencies
        run: npm install --no-audit --no-fund
        working-directory: learnquest/backend

      - name: Syntax + project validation (npm run check)
        run: npm run check

      - name: Unit tests (npm test)
        run: npm test
        working-directory: learnquest/backend
